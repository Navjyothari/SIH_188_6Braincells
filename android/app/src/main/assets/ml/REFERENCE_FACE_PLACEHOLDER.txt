STATUS: DEPLOYED AND VERIFIED.

The synthetic reference face image (synthetic_reference_face.jpg, 512×512 px)
is placed in this directory (tracked in git, bundled into APK at build time).

Source:
  AI-generated synthetic frontal ID portrait (diffusion synthesis, fictional person).
  Zero biometric or personal data from any real individual.

Format:
  JPEG, 512×512 px RGB, quality 95 (~58 KB).
  Frontal headshot with visible eyes, nose, mouth landmarks detected by OpenCV/ML Kit.

This file is used ONLY as a development/demo reference for the optional M3 module.
In production, the reference face comes from a consented capture flow or e-Passport RFID chip,
never an unconsented bundled asset.

